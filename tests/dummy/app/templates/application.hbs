{{md-toasts}}
<div class="navbar-fixed">
{{#md-nav as |n|}}
  {{#n.title 'index'}}
    ember-cli-materialize {{/n.title}}
  {{#n.menu class='right' as |m|}}
    {{#m.btn text='Components' as |b|}}
      {{#b.dropdown as |d|}}
        {{d.link-to 'Button' 'button'}}
        {{d.link-to 'Card' 'card'}}
        {{d.link-to 'Collapsible' 'collapsible'}}
        {{d.link-to 'Collection' 'collection'}}
        {{d.link-to 'Forms' 'forms'}}
        {{d.link-to 'Loader' 'loader'}}
        {{d.link-to 'Nav' 'nav'}}
        {{d.link-to 'Pagination' 'pagination'}}
        {{d.link-to 'Side-Nav' 'sidenav'}}
        {{d.link-to 'Modal' 'modal'}}
        <li class="divider"></li>
        {{d.link-to 'Toasts' 'toasts'}}
      {{/b.dropdown}}
    {{/m.btn}}
  {{/n.menu}}
{{/md-nav}}
</div>

<div class="container">
  <div class="row">
    <div class="col s12">
      {{outlet}}
    </div>
  </div>
</div>
